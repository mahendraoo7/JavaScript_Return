
/*
        //  DOM manipulation : Attributes

            getAttributes(attr)          // to get attributes value 
            setAttributes(attr,value)    //  to set the attributes val th  
            classList.add .remove   // to add new class and remove class 
         



          DOM Style 
          
            document.body.style.visibility = "hidden"
            val.style.display = "none"
            document.body.style.color = "red"
            document.body.style.backgroundColor = "red"
            

           DOM Insert Elements

            document.createaElement("button")                 //   append for starting apply   // pepend for ending apply  
            node.append(el) // adds at the end of nods(inside ) 
            node.prepend(el)  // adds at the start of node (inside)
            node.before(el)  // add before the node (outside)
            node.after(el)   //  add after the node(outside) 


           
           


*/    