alert("hello");


  /* VERIABLE 

  1 var
    var can be re-declared and updated 
    var is global scope variable
    var remove in 2015 ES6 and add new veriable Let and const 

  2 let 
    let is cannot be re declared and but can be updated 
    let is block scope variable 

  3 const 
     const is cannot be re-declare and update 
     const is block scope veriable 
     

*/

/*   DATATYPE 

  A : Premitive Datatype 
    1. Number
    2. String
    3. Boolean
    4. Undefine
    6. Null
    7. bigInt
    8. Symbol

  B : Non-premitive  
    1. Object 
    2. Array


        *** const is not update but const object key can update
*/